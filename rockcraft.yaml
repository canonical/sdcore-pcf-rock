name: sdcore-pcf
base: bare
build-base: ubuntu@24.04
version: '1.4.2'
summary: SD-Core PCF
description: SD-Core PCF
license: Apache-2.0
platforms:
  amd64:
  arm64:

parts:
  pcf:
    plugin: go
    source: https://github.com/omec-project/pcf.git
    source-type: git
    source-tag: v1.4.2
    build-snaps:
      - go/1.21/stable
    stage-packages:
      - libc6_libs
      - base-files_lib
    organize:
      bin/cmd: bin/pcf
